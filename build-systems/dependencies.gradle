ext {
    // SDK versions — only bump minSdk in this PR; align compile/target to match.
    compileSdkVersion = 34
    targetSdkVersion = 34
    minSdkVersion = 28
    // Build tools
    buildToolsVersion = '34.0.0'
    // Kotlin / AGP bumps intentionally deferred to a separate PR to keep this
    // change focused on the minimum SDK bump.
}