apply from: "$rootDir/build-systems/apps-library.gradle"
dependencies {
    implementation project(":core-android")
    rootProject.detailDependencies.each {
        add(it.configuration, it.dependency, it.options)
    }
}